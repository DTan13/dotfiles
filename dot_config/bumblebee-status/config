[core]
modules = title,cpu,memory,nic,network_traffic,datetime,battery,brightness,amixer,blugon,uptime
theme = iceberg-dark-powerline

[module-parameters]
interval=1
cpu.warning=50
cpu.critical=70

title.max=40
title.short=true

datetime.format=%a %d-%m %H:%M:%S

nic.exclude=lo,virbr,docker,vboxnet,veth,vmnet,br
nic.include=eno,enp,ens,enx,eth,wlan,wlp
nic.states=^down

memory.warning=80
memory.critical=90

amixer.percent_change=2
